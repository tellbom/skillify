---
name: feature
description: Implement a scoped feature with a configurable plan approval gate and TDD.
---

# Feature Development

Clarify the acceptance criteria, locate evidence in the repository, and produce a focused
plan before editing. Web-originated runs require explicit plan approval by default. Implement
with a failing test first where practical, then review the final diff against the criteria.
