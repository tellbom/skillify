workflowVersion: 1
id: behavior-preserving-refactor
runtime: [opencode, claude-code]
entryAgent: build
mode: workspace-write
skills: [behavior-baseline, behavior-preserving-refactor, verification-before-completion, requesting-code-review]
gates:
  - id: baseline-recorded
    requiredByDefault: true
artifacts:
  - refactor-report.json
  - change.patch
